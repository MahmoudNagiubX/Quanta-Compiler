# Quanta Programming Language

Quanta is a modern, compiled programming language and ecosystem built from scratch.

## Project Structure
- `compiler/`: Core compiler pipeline (Lexer, Parser, AST, Semantic, IR, Optimizer, Backend)
- `examples/`: Example `.qn` programs
- `tests/`: Test suites for all compiler phases
